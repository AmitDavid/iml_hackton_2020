task1
    src
        Classification.py -
            Include random forest classification model.

        model.py -
            Include main class which runs the model.

        preprocess_flight_data.py -
            Include function which process the features given by X matrix.

        Regression.py -
            Include decision tree regression model.

        train_script.py -
            Used by us only, train and output the model with given train data.

        weather_preprocess.py -
            Include function which process the weather features.

    pickle
        class_model -
            file conatins trained classification model.

        reg_model -
            file conatins trained regression model.

README.txt -
    this file.